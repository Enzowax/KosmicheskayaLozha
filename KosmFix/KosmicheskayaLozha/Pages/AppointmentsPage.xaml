<Page x:Class="KosmicheskayaLozha.Pages.AppointmentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BackgroundBrush}" Title="Записи">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Шапка -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="16,10">
                <Button Content="← Назад" Style="{StaticResource BackButton}"
                        HorizontalAlignment="Left" Click="BtnBack_Click"/>
                <TextBlock x:Name="TxtTitle" Foreground="{StaticResource AccentBrush}"
                           FontSize="18" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Фильтр по дате -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" Padding="16,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Дата:" Foreground="{StaticResource SubtextBrush}"
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                <DatePicker x:Name="DpDate" Background="#3D1A6E"
                            Foreground="{StaticResource TextBrush}"
                            SelectedDateChanged="DpDate_Changed"/>
                <Button Content="Сбросить" Style="{StaticResource BackButton}"
                        Margin="12,0,0,0" Click="BtnResetDate_Click"/>
            </StackPanel>
        </Border>

        <!-- Список слотов -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="20,16">
            <WrapPanel x:Name="WpSlots" x:FieldModifier="private"/>
        </ScrollViewer>
    </Grid>
</Page>
