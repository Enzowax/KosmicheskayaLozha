<Page x:Class="KosmicheskayaLozha.Pages.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BackgroundBrush}" Title="Товары">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="16,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Content="← Назад" Style="{StaticResource BackButton}"
                        VerticalAlignment="Center" Click="BtnBack_Click"/>
                <TextBlock Grid.Column="1" Text="🛍 Косметика"
                           Foreground="{StaticResource AccentBrush}"
                           FontSize="20" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button Grid.Column="2" Content="🛒 Корзина"
                        Style="{StaticResource PrimaryButton}"
                        VerticalAlignment="Center" Click="BtnCart_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" Padding="16,10"
                BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="0,0,0,1">
            <WrapPanel>
            
                <TextBox x:Name="TxtSearch" Width="200" Margin="0,0,12,0"
                         TextChanged="Filter_Changed">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush AlignmentX="Left" Stretch="None">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="🔍 Поиск по названию..."
                                                               Foreground="#7a5da8" Margin="9,6"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <ComboBox x:Name="CmbType" Style="{StaticResource ModernComboBox}"
                          Width="160" Margin="0,0,12,0" DisplayMemberPath="Name"
                          SelectionChanged="Filter_Changed"/>

                <ComboBox x:Name="CmbManufacturer" Style="{StaticResource ModernComboBox}"
                          Width="180" Margin="0,0,12,0" DisplayMemberPath="Name"
                          SelectionChanged="Filter_Changed"/>

                <Button Content="Сбросить" Style="{StaticResource BackButton}"
                        Click="BtnReset_Click"/>
            </WrapPanel>
        </Border>

        <!-- Сетка товаров -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="WpProducts" Margin="16"/>
        </ScrollViewer>
    </Grid>
</Page>
